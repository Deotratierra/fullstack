br
.container
  .row
    .col.s6
      br
      br
      // Creamos el contenedor svg
      svg(height="250" width="350" style="border:1px solid black")
        // Añadimos un camino (figura azul)
        path(d="M50,200 C50,50 300,50 300,200" fill="#1a237e")
        
        // Coordenadas de referencia
        circle(cx="50" cy="50" r="5" fill="red")
        circle(cx="300" cy="50" r="5" fill="green")
        circle(cx="50" cy="200" r="5" fill="orange")
        circle(cx="300" cy="200" r="5" fill="purple")
      h6
        b Ruta del ejemplo:
      p <code>d="M<span style="color:orange">50,200</span> C<span style="color:red">50,50</span> <span style="color:green">300,50</span> <span style="color:purple">300,200</span>"</code>
      br
      svg(height="250" width="350" style="border:1px solid black")
        // Añadimos un camino (figura azul)
        path(d="M50,200 C50,50 300,50 300,200" fill="none" stroke="#1a237e" stroke-width="3px" )
      
    .col.s6
      h5
        a(href="https://es.wikipedia.org/wiki/Curva_de_B%C3%A9zier" target="__blank") Curvas Bézier
      p Para crear curvas Bézier en SVG usamos una ruta (<code>path</code>) dentro del contenedor <code>svg</code>. 
      p Primero nos movemos con <code>M</code> al principio de la ruta. Luego con <code>C</code> (comando de curvas) creamos dos puntos de control (anclas) que determinarán la curva. Finalmente creamos otro punto que será donde termine nuestra curva.
      a(href="https://developer.mozilla.org/en-US/docs/Web/SVG/Tutorial/Paths" target="__blank") Referencia de rutas en SVG
      br
      br      
      hr
      br
      br
      center
        a.btn.cyan.darken-3(href="https://codepen.io/mondeja/post/dibujo-vectorial-con-svg" target="__blank") Volver al índice
        a.btn.indigo.darken-4(href="https://codepen.io/mondeja/pen/ooJNXJ" target="__blank") Concatenar Béziers ->
      br
      br
      hr
      br
      p Si queremos sólo una curva, sin relleno, podemos especificar:
      p
        code fill="none" 
      p
        code stroke="&lt;color>"
      p
        code stroke-width="&lt;ancho_de_linea>"</code>

      
            