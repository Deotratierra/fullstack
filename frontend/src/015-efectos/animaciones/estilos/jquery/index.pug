br
.container
  .row
    .col.s6
      a.btn.green#grande Grande
      a.btn.red#enano Pequeño
      p#texto Puedes cambiarme el tamaño
    .col.s6
      h5 Animar elementos
      p Con la función <code>animate()</code> podemos cambiar el estilo de los elementos mediante una transición controlada. Toma 3 parámetros, como primero un objeto con los atributos de estilo y valor (los atributos de estilo deben ir en camelCase, por ejemplo: <code>font-size</code> sería <code>fontSize</code>), como segundo la velocidad de transición y como tercero un callback.
      p Ciertos elementos no cuentan con transiciones en la biblioteca estándar, si no que necesitamos otros plugins que proveen esa funcionalidad.
      a(href="https://www.w3schools.com/jquery/eff_animate.asp") Referencia de la función <code>animate()</code>