#!/bin/bash

# ===============================================

# Saber que tipo es una orden de Bash
type if    # if es una palabra clave del shell
type exit  # exit es una orden interna del shell

# ===============================================