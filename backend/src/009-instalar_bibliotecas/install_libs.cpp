
/* INSTALACIÓN Y LOCALIZACIÓNN DE BIBLIOTECAS EN C++ */

int main() {
    /* En sistemas Linux, las bibliotecas estándar de C++
    se encuentran en el directorio /usr/include/c++/<version>
    y se les denomina headers.

    Para saber que version del compilador estamos usando,
    podemos ejecutar: g++ --version

    Cuando ejecutamos #include <biblioteca>, el compilador
    las busca en las carpetas que tiene configuradas para buscar.
    Podemos consultar cuales son con el comando: cpp -v
    */
	return 0;
}